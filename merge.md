## Git discrd local changes

```
git reset --hard
```

## Git refusing to merge unrelated histories on rebase

```
git pull --allow-unrelated-histories
```

OR

```
git pull <Branch Name> --allow-unrelated-histories
```
